# 📘 Lecture 2 — Word Embeddings II (Thomas Lukasiewicz, 2024W)

> **Тема:** от word2vec к счётным методам (count-based), SVD, GloVe; оптимизация (gradient descent/SGD); оценки качества эмбеддингов (intrinsic/extrinsic); многозначность слов (polysemy).

---

## 0) Быстрый обзор (review) Skip-gram
**Идея:** модель **skip-gram** учится предсказывать слова контекста по центральному слову, максимизируя правдоподобие наблюдаемых пар *(center, context)* в скользящем окне (context window).

Обозначения:
- $w_t$ — центральное слово (center word)
- $C$ — радиус окна (window size)
- $\mathbf{v}_w$ — входной вектор слова $w$ (input embedding)
- $\mathbf{u}_w$ — выходной вектор слова $w$ (output embedding)
- $V$ — размер словаря (vocabulary size)

**Skip-gram (softmax):**
\[
p(w_{t+j}\mid w_t)=
\frac{\exp(\mathbf{u}^\top_{w_{t+j}}\mathbf{v}_{w_t})}
{\sum_{w=1}^{V}\exp(\mathbf{u}_w^\top\mathbf{v}_{w_t})}\,,
\quad j\in\{-C,\dots,-1,1,\dots,C\}
\]
\[
\mathcal{L}_{\text{SG}}=\sum_{t=1}^{T}\sum_{\substack{-C\le j\le C\\ j\ne 0}}
\log p(w_{t+j}\mid w_t)
\]

**Negative Sampling (NS):** заменить полную нормализацию $V$ на $K\!\ll\!V$ «негативов» (negative samples):
\[
\log\sigma(\mathbf{u}_{w_o}^\top\mathbf{v}_{w_i})
+\sum_{k=1}^{K}\mathbb{E}_{w_k\sim P_n}\big[\log\sigma(-\mathbf{u}_{w_k}^\top\mathbf{v}_{w_i})\big]
\]

---

## 1) Простая оптимизация: Gradient Descent / SGD

### 1.1 Градиентный спуск (gradient descent)
Ищем параметры $\theta$ (все $\mathbf{v},\mathbf{u}$), минимизирующие функцию потерь $J(\theta)$:
\[
\theta\leftarrow\theta-\eta\,\nabla_\theta J(\theta)
\]
где $\eta$ — скорость обучения (learning rate).

### 1.2 Стохастический градиентный спуск (SGD)
Обновление на мини-батче (mini-batch) или одном примере:
\[
\theta\leftarrow\theta-\eta\,\nabla_\theta J_i(\theta)
\]
Плюсы: дешёво по памяти, быстро даёт прогресс; Минусы: шумные шаги → нужны scheduler/моментум/Adam, градиентный клиппинг.

### 1.3 Псевдокод обновления (negative sampling, упрощённо)
```python
# w_i = center, w_o = true context, negs = K negative words
score_pos = dot(u[w_o], v[w_i])         # u·v
loss = -log(sigmoid(score_pos))
for w_k in negs:
    loss += -log(sigmoid(-dot(u[w_k], v[w_i])))

# Backprop: grads wrt v[w_i], u[w_o], {u[w_k]}
# v[w_i] <- v[w_i] - lr * dL/dv[w_i]
# u[w_o] <- u[w_o] - lr * dL/du[w_o]
# u[w_k] <- u[w_k] - lr * dL/du[w_k]

---

## 2) Почему не всегда предсказательные модели? Счётные методы (count-based)

**Вопрос:** «почему бы не захватывать совместные встречаемости напрямую?» (co-occurrence counts).  
Идея: строим **матрицу совместных встречаемостей** \(X \in \mathbb{R}^{V \times V}\), где \(X_{ij}\) — сколько раз слово \(j\) встречалось в окрестности слова \(i\) (окно — context window), иногда с весом \(1/\text{distance}\).

### 2.1 Window-based co-occurrence
Пример: предложение *“the cat sat on the mat”*, окно \(C=2\). Для каждого слова учитываем соседей на расстоянии \(\le2\).

### 2.2 Проблемы «сырых» счётчиков
- **Высокая размерность** и **разреженность** (sparsity).  
- Плохая генерализация для **редких слов**.  
- Сверхчастые токены («the», «of», …) доминируют — «шум» для семантики.

---

## 3) Понижение размерности (dimensionality reduction): SVD

Идея: применить **SVD** (сингулярное разложение) к \(X\), получить компактные векторы.

\[
X \approx U_k\,\Sigma_k\,V_k^\top,
\]
где \(U_k, V_k\) — ортонормальные, \(\Sigma_k\) — диагональная с топ-\(k\) сингулярами.  
**Эмбеддинг слова** часто берут как строку из \(U_k\Sigma_k^{1/2}\) (эмбеддинг контекста — \(V_k\Sigma_k^{1/2}\)).

### 3.1 PPMI как «хак» к \(X\)
Вместо «сырых» частот используют **PPMI** (positive PMI):

\[
\operatorname{PMI}(i,j)=\log\frac{p(i,j)}{p(i)p(j)},\qquad
\operatorname{PPMI}(i,j)=\max\{\operatorname{PMI}(i,j),0\},
\]

где \(p(i,j)=\frac{X_{ij}}{\sum_{i'j'} X_{i'j'}}\), \(p(i)=\frac{\sum_j X_{ij}}{\sum_{i'j'} X_{i'j'}}\), \(p(j)=\frac{\sum_i X_{ij}}{\sum_{i'j'} X_{i'j'}}\).

---

## 4) Count-based vs Predictive (связи и различия)

- **Count-based**: (PPMI-)SVD над глобальными статистиками.  
- **Predictive**: word2vec/SGNS учит векторы, **предсказывая** соседей.  
- Связь: SGNS приближённо факторизует **PMI-сдвиги**; различаются процедурой оптимизации и масштабируемостью.

---

## 5) GloVe (Global Vectors for Word Representation)

**Интуиция:** использовать **глобальные** \(X_{ij}\) и связать **разности векторов** с **отношениями частот**. Логарифм совместной частоты \(\log X_{ij}\) — «естественная» величина.

### 5.1 Целевая функция (loss)
\[
J = \sum_{i,j=1}^{V} f(X_{ij})
\Big(\mathbf{w}_i^\top \tilde{\mathbf{w}}_j + b_i + \tilde b_j - \log X_{ij}\Big)^2,
\]

где \(\mathbf{w}_i\) — вектор слова, \(\tilde{\mathbf{w}}_j\) — вектор контекста, \(b_i,\tilde b_j\) — смещения, а весовая функция:

\[
f(x)=
\begin{cases}
\left(\dfrac{x}{x_{\max}}\right)^\alpha, & x < x_{\max}\\[4pt]
1, & x \ge x_{\max}
\end{cases}
\quad (\alpha\approx 0.75,\ x_{\max}\approx 100).
\]

### 5.2 Градиенты (для пары \((i,j)\))
\[
\delta_{ij}=\mathbf{w}_i^\top \tilde{\mathbf{w}}_j + b_i + \tilde b_j - \log X_{ij},
\]
\[
\frac{\partial J}{\partial \mathbf{w}_i}=2\,f(X_{ij})\,\delta_{ij}\,\tilde{\mathbf{w}}_j,\qquad
\frac{\partial J}{\partial \tilde{\mathbf{w}}_j}=2\,f(X_{ij})\,\delta_{ij}\,\mathbf{w}_i,
\]
и аналогично по \(b_i,\tilde b_j\).  
Итоговый вектор слова часто берут как \(\tfrac{1}{2}(\mathbf{w}_i+\tilde{\mathbf{w}}_i)\).

---

## 6) Всплывающие закономерности (emergent patterns)

- **Синтаксические** кластеры (syntactic).  
- **Семантические** кластеры (semantic), аналогии.  

**Аналогии (3CosAdd):**
\[
\arg\max_{w}\ \cos\!\big(\mathbf{v}_w,\ \mathbf{v}_{b}-\mathbf{v}_{a}+\mathbf{v}_{c}\big).
\]

**Вариант (3CosMul):**
\[
\arg\max_{w}\ 
\frac{\cos(\mathbf{v}_w,\mathbf{v}_b)\cdot\cos(\mathbf{v}_w,\mathbf{v}_c)}
{\cos(\mathbf{v}_w,\mathbf{v}_a)+\varepsilon}.
\]

---

## 7) Оценка эмбеддингов (evaluation)

### 7.1 Внутренние (intrinsic)
- **Ближайшие соседи** (nearest neighbors; cosine).  
- **Аналогии** (word analogies): accuracy@k, влияние \(d\), \(C\), \(K\).  
- **Сходство/родство** (similarity/relatedness): ранговая корреляция (Spearman) с человеческими скорами (WordSim-353, MEN, SimLex-999).

**Косинусное сходство:**
\[
\cos(\mathbf{a}, \mathbf{b})=\frac{\mathbf{a}^\top\mathbf{b}}{\lVert\mathbf{a}\rVert_2\,\lVert\mathbf{b}\rVert_2}.
\]

### 7.2 Внешние (extrinsic)
Используем эмбеддинги как признаки в реальных задачах: **NER**, **POS-tagging**, **sentiment**, **parsing**, **QA**, **IR** — считаем метрики задачи (F1/accuracy/BLEU и т.п.).

---

## 8) Многозначность (polysemy) и несколько прототипов

Обычные word2vec/GloVe дают **один** вектор на лексему → смешение значений. Подходы:
- **Global context** (учёт темы/документа),
- **Несколько прототипов** (multiple prototypes): кластеризация контекстов и обучение нескольких векторов на слово.  
Современный ответ — **контекстные эмбеддинги** (ELMo, BERT): вектор зависит от конкретного окружения токена.

---

## 9) Практические советы (practical tips)

- **Размерность \(d\)**: 100–300–768+ (компромисс качество/ресурсы).  
- **Окно \(C\)**: 2–10 (малое → синтаксис; большое → тематика).  
- **Negative samples \(K\)**: 5–20; **subsampling** частых слов.  
- **Стабилизация**: gradient clipping, early stopping, адекватная инициализация, фиксируй seed.  
- **Оценка**: делай и **intrinsic**, и **extrinsic**.

---

## 10) Мини-практикум (что уметь руками)

**A. PPMI → Truncated SVD**  
1) Построить \(X\) по окну; 2) Преобразовать в **PPMI**; 3) Применить **TruncatedSVD** (randomized) на разреженной матрице; 4) Нормировать, проверить neighbors/analogies.

**B. Skip-gram + NS (скелет шага)**  
1) Для \((\text{center}, \text{context})\) обновить \(\mathbf{v}_{\text{center}}, \mathbf{u}_{\text{context}}\).  
2) Для негативов \(w_k\sim P_n\) обновить \(\mathbf{u}_{w_k}\) и \(\mathbf{v}_{\text{center}}\).  
3) Эпохи, lr-scheduler, early-stopping.

**C. GloVe (игрушечный шаг)**  
Для каждой тройки \((i,j,X_{ij})\) посчитать \(f(X_{ij})\), \(\delta_{ij}\), сделать шаги по \(\mathbf{w}_i,\tilde{\mathbf{w}}_j,b_i,\tilde b_j\); в конце \( (\mathbf{w}_i+\tilde{\mathbf{w}}_i)/2 \).

---

## 11) Мини-шпаргалка формул

**PMI/PPMI:**
\[
\operatorname{PMI}(i,j)=\log\frac{p(i,j)}{p(i)p(j)},\qquad
\operatorname{PPMI}(i,j)=\max\{\operatorname{PMI}(i,j),0\}.
\]

**SVD (ранг-\(k\)) и представления:**
\[
X\approx U_k\Sigma_k V_k^\top,\quad
\text{word}\approx \text{row}(U_k\Sigma_k^{1/2}),\ 
\text{context}\approx \text{row}(V_k\Sigma_k^{1/2}).
\]

**GloVe loss:**
\[
J=\sum_{i,j} f(X_{ij})
\big(\mathbf{w}_i^\top\tilde{\mathbf{w}}_j+b_i+\tilde b_j-\log X_{ij}\big)^2.
\]

**3CosAdd (аналогии):**
\[
\arg\max_{w}\ \cos\!\big(\mathbf{v}_w,\ \mathbf{v}_{b}-\mathbf{v}_{a}+\mathbf{v}_{c}\big).
\]

**Cosine:**
\[
\cos(\mathbf{a},\mathbf{b})=\frac{\mathbf{a}^\top\mathbf{b}}
{\lVert\mathbf{a}\rVert_2\,\lVert\mathbf{b}\rVert_2}.
\]

---

## 12) Литература (для закрепления)

- **GloVe**: Pennington, Socher, Manning (EMNLP 2014).  
- **Levy et al. (2015)**: связь count-based и predictive.  
- **Schnabel et al. (2015)**: intrinsic-оценки эмбеддингов.  
- **Arora et al. (2016/2017/2018)**: PMI-подход и линейная структура смыслов.  
- **Yin et al. (2018)**: о размерности эмбеддингов.

---

## 🔎 Итог

Лекция выводит за рамки word2vec: **co-occurrence → PPMI → SVD**, вводит **GloVe** как регрессию к \(\log X_{ij}\) с весами, обсуждает **SGD/градиенты**, **оценку эмбеддингов** (intrinsic/extrinsic) и **многозначность** (polysemy). Это база для контекстных моделей (**ELMo/BERT**), где вектор зависит от **конкретного контекста** токена.

