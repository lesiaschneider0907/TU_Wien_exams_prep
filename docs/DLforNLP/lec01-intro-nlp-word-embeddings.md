---
layout: default
title: Lecture 1 — Introduction to NLP and Word Embeddings I

---
# 📘 Lecture 1 — Introduction to NLP and Word Embeddings I

> **Курс:** Deep Learning for NLP  
> **Тема:** Введение в NLP, распределённые представления и Word2Vec  
> **Формат:** Markdown с LaTeX-формулами (GitHub Math Support)

---

## 1) Что такое NLP (Natural Language Processing)

**NLP (обработка естественного языка)** — область на стыке **computer science**, **artificial intelligence**, **machine learning** и **linguistics**, цель которой — чтобы компьютеры могли **анализировать, понимать и генерировать** человеческий язык.

**Примеры задач (applications):**
- Поиск и классификация документов (**search, document classification**)
- Диалоги/ассистенты (**dialog systems, virtual assistants**)
- Машинный перевод (**machine translation, MT**)
- Речь (**automatic speech recognition, ASR**; **text-to-speech, TTS**)
- Аналитика тональности (**sentiment analysis**), тематическое моделирование (**topic modeling**)

---

## 2) Почему язык сложный для машин?

- Язык **символический (symbolic)**, а физические сигналы и мозговые представления — **непрерывные (continuous)**.  
- **Неоднозначность (ambiguity)**, контекст и фоновые знания мира.  
  Пример: *“I made her duck.”* — многозначность лексики и синтаксиса.
- Большой словарь → **разреженность (sparsity)** при дискретном кодировании (one-hot).

---

## 3) Почему Deep Learning (DL) подходит для NLP

- Переход от **ручных признаков (hand-engineered features)** к **обучаемым представлениям (learned representations)**.  
- DL стал возможен благодаря большим данным, **GPUs**, и новым моделям (**RNN/LSTM → Transformers**), а также **transfer learning** (BERT, GPT).
- Унифицированная обучаемая рамка **end-to-end** от токенов к задаче.

---

## 4) Что такое word embeddings (векторные представления слов)

**Distributional hypothesis (распределённая гипотеза):**  
*Слова, встречающиеся в похожих контекстах, имеют похожие значения.*

Вместо индекса слова представляем его **вектором** в $\mathbb{R}^d$:

\[
\text{word} \rightarrow \mathbf{v} \in \mathbb{R}^d
\]

- Похожие по смыслу слова имеют близкие векторы (**cosine similarity**).  
- Улучшают обобщение по сравнению с **one-hot**.

---

## 5) Word2Vec: основные идеи и режимы

**Цель:** Извлекать распределённую семантику из статистики совместных появлений слов.

Два режима:
- **CBOW (Continuous Bag-of-Words)** — предсказываем центральное слово по окружению (контексту).
- **Skip-gram** — предсказываем слова контекста по центральному слову.

Обозначения:
- $w_t$ — слово в позиции $t$  
- $C$ — размер окна (**context window**)  
- $V$ — размер словаря (**vocabulary size**)  
- $d$ — размерность эмбеддинга (**embedding dimension**)  
- $\mathbf{v}_w \in \mathbb{R}^d$ — «входной» вектор слова $w$  
- $\mathbf{u}_w \in \mathbb{R}^d$ — «выходной» вектор слова $w$

---

## 6) Формулы Word2Vec

### 6.1 CBOW (предсказываем центральное слово по контексту)

Контекстное множество для позиции $t$:
\[
\text{Context}(t) = \{\,w_{t-C}, \ldots, w_{t-1},\; w_{t+1}, \ldots, w_{t+C}\,\}
\]

Агрегированный контекстный вектор (среднее «входных» векторов):
\[
\mathbf{h} = \frac{1}{2C}\sum_{\substack{-C \le j \le C \\ j \ne 0}} \mathbf{v}_{w_{t+j}}
\]

Модельная вероятность правильного центрального слова $w_t$ (softmax):
\[
p(w_t \mid \text{Context}) =
\frac{\exp\!\left(\mathbf{u}_{w_t}^{\top}\mathbf{h}\right)}{\sum_{w=1}^{V} \exp\!\left(\mathbf{u}_{w}^{\top}\mathbf{h}\right)}
\]

Функция потерь (отрицательное лог-правдоподобие) суммируется по датасету.

---

### 6.2 Skip-gram (предсказываем контекст по центральному слову)

Вероятность наблюдать слово контекста $w_{t+j}$ при центральном $w_t$:
\[
p(w_{t+j} \mid w_t) =
\frac{\exp\!\left(\mathbf{u}_{w_{t+j}}^{\top}\mathbf{v}_{w_t}\right)}
{\sum_{w=1}^{V} \exp\!\left(\mathbf{u}_{w}^{\top}\mathbf{v}_{w_t}\right)}
\]

Суммарное лог-правдоподобие (максимизируем):
\[
\mathcal{L}_{\text{SG}} =
\sum_{t=1}^{T} \sum_{\substack{-C \le j \le C \\ j \ne 0}}
\log p(w_{t+j} \mid w_t)
\]

---

### 6.3 Ускорение: Negative Sampling (NS)

Полный softmax дорог при большом $V$. **Negative Sampling (NS)** заменяет нормализацию на несколько «негативных» примеров ($K \ll V$).

Для пары $(w_i, w_o)$ (input, output) оптимизируем:
\[
\log \sigma\!\left(\mathbf{u}_{w_o}^{\top}\mathbf{v}_{w_i}\right)
\;+\;
\sum_{k=1}^{K}
\mathbb{E}_{w_k \sim P_n(w)}
\Big[\log \sigma\!\left(-\,\mathbf{u}_{w_k}^{\top}\mathbf{v}_{w_i}\right)\Big]
\]
где $\sigma(x)=\frac{1}{1+e^{-x}}$ — сигмоида, $P_n(w)$ — распределение для отбора негативов.

---

### 6.4 Косинусное сходство (cosine similarity)

Для оценки семантической близости:
\[
\cos(\mathbf{a}, \mathbf{b}) =
\frac{\mathbf{a}^{\top}\mathbf{b}}{\lVert \mathbf{a}\rVert_2 \,\lVert \mathbf{b}\rVert_2}
\]

---

## 7) Почему векторы «работают» везде

- **Единый формат представления** (векторы/тензоры) удобен для слов, морфологии, синтаксиса, семантики, и для высокоуровневых задач (sentiment, QA, MT, dialogue).  
- **Обучаемость и перенос** (transfer): один раз обученные эмбеддинги улучшают множество задач.  
- **Снижают потребность в hand-crafted features**.

---

## 8) CBOW vs Skip-gram — когда что

- **CBOW:** быстрее, сглаживает контексты, лучше на частых словах.  
- **Skip-gram:** лучше на редких словах, детальнее моделирует окрестности, но дороже.

---

## 9) Практические заметки

- **Окно (context window)**: фиксированное $C$ или случайное до $C_\text{max}$.  
- **Подвыборка частых слов (subsampling)**: уменьшает шум от сверхчастых токенов.  
- **NS параметр $K$**: обычно 5–20.  
- **Инициализация/оптимизация**: SGD / Adam, обучение нескольких эпох.

---

## 10) Мини-шпаргалка формул (копипаста для GitHub)

**Softmax (общая форма):**
\[
\operatorname{softmax}(\mathbf{z})_i = \frac{e^{z_i}}{\sum_j e^{z_j}}
\]

**CBOW вероятность:**
\[
p(w_t \mid \text{Context}) =
\frac{\exp\!\left(\mathbf{u}_{w_t}^{\top}\mathbf{h}\right)}{\sum_{w=1}^{V} \exp\!\left(\mathbf{u}_{w}^{\top}\mathbf{h}\right)}
\]

**Skip-gram вероятность:**
\[
p(w_{t+j} \mid w_t) =
\frac{\exp\!\left(\mathbf{u}_{w_{t+j}}^{\top}\mathbf{v}_{w_t}\right)}
{\sum_{w=1}^{V} \exp\!\left(\mathbf{u}_{w}^{\top}\mathbf{v}_{w_t}\right)}
\]

**Negative Sampling (одна пара + K негативов):**
\[
\log \sigma\!\left(\mathbf{u}_{w_o}^{\top}\mathbf{v}_{w_i}\right)
+
\sum_{k=1}^{K}
\mathbb{E}_{w_k \sim P_n(w)} \Big[\log \sigma\!\left(-\,\mathbf{u}_{w_k}^{\top}\mathbf{v}_{w_i}\right)\Big]
\]

**Cosine similarity:**
\[
\cos(\mathbf{a}, \mathbf{b}) =
\frac{\mathbf{a}^{\top}\mathbf{b}}{\lVert \mathbf{a}\rVert_2 \,\lVert \mathbf{b}\rVert_2}
\]

---

## 11) Чего знать к следующей лекции

- Разница **CBOW vs Skip-gram** (скорость/редкие слова)  
- Что такое **distributional hypothesis**  
- Как работает **softmax** и **negative sampling**  
- Почему embeddings лучше **one-hot**  
- Базовые метрики: **cosine similarity**

---

## 12) Дополнительно

- Mikolov et al. (2013): *Efficient Estimation of Word Representations in Vector Space*  
- Mikolov et al. (2013): *Distributed Representations of Words and Phrases and their Compositionality*  
- Практика: Gensim / PyTorch реализация Skip-gram/CBOW с NS

---

### 📂 Рекомендованная структура репозитория

